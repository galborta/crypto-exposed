<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Crypto Exposed</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <%- include('partials/header') %>

    <main class="container mx-auto px-4 py-8">
        <div class="max-w-2xl mx-auto">
            <div class="bg-white rounded-lg shadow-lg p-8 text-center">
                <h1 class="text-3xl font-bold text-red-600 mb-4">Error</h1>
                <p class="text-xl text-gray-700 mb-6"><%= message %></p>
                <% if (error && Object.keys(error).length > 0) { %>
                    <pre class="bg-gray-100 p-4 rounded text-left text-sm overflow-auto"><%= JSON.stringify(error, null, 2) %></pre>
                <% } %>
                <div class="mt-8">
                    <a href="/" class="bg-gray-900 text-white px-6 py-3 rounded-lg hover:bg-gray-800 inline-block">
                        Return to Homepage
                    </a>
                </div>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>
</body>
</html> 